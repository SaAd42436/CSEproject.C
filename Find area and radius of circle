#include <stdio.h>

int main() {
    float r, area;

    printf("Enter radius: ");
    scanf("%f", &r);

    area = 3.1416 * r * r;  // Area formula: πr²

    printf("Area: %.2f\n", area);
    printf("Radius: %.2f\n", r);

    return 0;
}
